# nova-btreedb

Driver for BTreeDB (Nova's embedded B-tree engine) over its binary protocol. A Nova package — fetch with:

```sh
nova get https://github.com/kamlesh-nb/nova-btreedb
```

```nova
import btreedb;
```
